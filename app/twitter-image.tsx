/**
 * The Twitter/X card image reuses the Open Graph image so the share visual is
 * identical across platforms and defined once. Pairs with the
 * summary_large_image card set in the metadata builder.
 */

export { default, alt, size, contentType } from "./opengraph-image";
